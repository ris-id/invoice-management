create table payment_provider(
    id varchar(36),
    primary key(id)
);

create table invoide_type(
    id varchar(36),
    primary key(id)
);

create table invoice(
    id varchar(36),
    primary key(id)
);

create table virtual_account(
    id varchar(36),
    primary key(id)
);

create table payment(
    id varchar(36),
    primary key(id)
);